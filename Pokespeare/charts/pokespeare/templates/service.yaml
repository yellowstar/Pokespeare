apiVersion: v1
kind: Service
metadata:
  name: {{ template "pokespeare.fullname" . }}
  labels:
    app: {{ template "pokespeare.name" . }}
    chart: {{ template "pokespeare.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "pokespeare.name" . }}
    release: {{ .Release.Name }}
